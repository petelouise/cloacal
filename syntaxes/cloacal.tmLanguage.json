{
	"name": "cloacal",
	"scopeName": "source.cloacal",
	"fileTypes": [ "clo" ],
	"patterns": [
		{
			"name": "meta.name-box.cloacal",
			"begin": "^\\s*\\+([-]+)\\+\\s*$",
			"end": "^\\s*\\+([-]+)\\+\\s*$",
			"beginCaptures": { "1": { "name": "punctuation.definition.box.cloacal" } },
			"endCaptures": { "1": { "name": "punctuation.definition.box.cloacal" } },
			"patterns": [
				{
					"match": "^\\s*\\|\\s*([^|]+?)\\s*\\|\\s*$",
					"captures": {
						"0": { "name": "meta.name-line.cloacal" },
						"1": { "name": "entity.name.character.cloacal" }
					}
				}
			]
		},
		{
			"name": "meta.key-value.cloacal",
			"match": "^\\s*(\\w+)\\s*([-~>*]+)\\s*(.+)$",
			"captures": {
				"1": { "name": "variable.other.key.cloacal" },
				"2": { "name": "string.unquoted.value.cloacal" }
			}
		},
		{
			"name": "meta.block-header.cloacal",
			"match": "^\\s*(\\w+)\\s*([-~>*]+)\\s*$",
			"captures": { "1": { "name": "keyword.control.block.cloacal" } }
		},
		{
			"name": "meta.list-item.cloacal",
			"begin": "^\\s*>\\s+",
			"end": "$",
			"patterns": [
				{
					"match": ".+",
					"name": "markup.list.item.cloacal"
				}
			]
		},
		{
			"name": "meta.block-content.cloacal",
			"begin": "^\\s{2,}(?!>)",
			"end": "$",
			"patterns": [
				{
					"match": ".+",
					"name": "string.unquoted.block-content.cloacal"
				}
			]
		}
	]
}
